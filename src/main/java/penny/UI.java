package penny;

/**
 * Handles display of all text from penny in the CLI.
 */
public class UI {
    /**
     * Displays text from penny in the CLI.
     *
     * @param text text to display.
     */
    public static void display(String text) {
        System.out.println(text);
    }
}
